diff --git a/src/lib/canary.ts b/src/lib/canary.ts
new file mode 100644
--- /dev/null
+++ b/src/lib/canary.ts
@@ -0,0 +1,5 @@
+// Scripted-session fixture (plan-002 U3): a known-good worker diff. The
+// canary test proves it survives the real gate (tsc strict, eslint, vitest).
+export function canaryGreeting(name: string): string {
+  return `hello, ${name}`
+}
diff --git a/src/lib/canary.test.ts b/src/lib/canary.test.ts
new file mode 100644
--- /dev/null
+++ b/src/lib/canary.test.ts
@@ -0,0 +1,7 @@
+import { expect, test } from 'vitest'
+
+import { canaryGreeting } from './canary'
+
+test('canary fixture compiles and passes the gate', () => {
+  expect(canaryGreeting('af')).toBe('hello, af')
+})
