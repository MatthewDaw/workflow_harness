"""Agent-families — a self-improving skill library for agent training.

Phase 0 (this build) is the standalone library core: the SQLite data model,
the ``add_idea`` registration pipeline, the insight lifecycle, skill rendering,
and SKILL.md export. No pipeline, explorer, grader, or reflector yet — those are
Plans 2-4.
"""

__version__ = "0.0.0"
