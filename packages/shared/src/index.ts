// @harness/shared — single source of truth for the event schema, API DTOs,
// and scope-resolution logic shared across backend, web, and (via golden
// fixtures) the Go wrapper.
//
// Real contents land in U2. This placeholder keeps the workspace buildable.

export const SHARED_PACKAGE_VERSION = '0.1.0';
