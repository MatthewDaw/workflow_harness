{
  "name": "workflow-harness",
  "version": "0.1.0",
  "private": true,
  "description": "claude+ and Command HQ — a harness for running and watching Claude workflows",
  "workspaces": [
    "packages/*",
    "infra"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,green \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run build -w @harness/shared && npm run dev -w @harness/backend",
    "dev:web": "npm run dev -w @harness/web",
    "build": "npm run build -w @harness/shared && npm run build -w @harness/backend && npm run build -w @harness/web",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "concurrently": "^9.1.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
